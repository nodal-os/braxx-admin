import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function CampaignPage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Campaign"
      body="No campaigns are loaded. This room stays empty until real plans exist."
    />
  );
}
