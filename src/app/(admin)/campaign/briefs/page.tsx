import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function CampaignBriefsPage() {
  return (
    <EmptyRoom
      kicker="Campaign"
      title="Briefs"
      body="No briefs are loaded. This room stays empty until real assignments exist."
    />
  );
}
