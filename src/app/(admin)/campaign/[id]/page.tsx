import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function CampaignDetailPage() {
  return (
    <EmptyRoom
      kicker="Campaign"
      title="Campaign"
      body="No campaign is loaded for this route."
    />
  );
}
