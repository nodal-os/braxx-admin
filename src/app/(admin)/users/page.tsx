import { EmptyRoom } from "@/components/workspace";

export default function UsersPage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Users"
      body="No team roster is loaded. Roles exist as structure. People stay empty until accounts are created."
    />
  );
}
