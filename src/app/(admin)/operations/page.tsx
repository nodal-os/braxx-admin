import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function OperationsPage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Operations"
      body="No tasks, tickets, or workflows are loaded. This room stays empty until operations data is wired."
    />
  );
}
