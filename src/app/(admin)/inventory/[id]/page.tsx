import { EmptyRoom } from "@/components/workspace";

export default function InventoryDetailPage() {
  return (
    <EmptyRoom
      kicker="Inventory"
      title="Item"
      body="No inventory item is loaded for this route."
    />
  );
}
