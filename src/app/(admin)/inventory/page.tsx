import { EmptyRoom } from "@/components/workspace";

export default function InventoryPage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Inventory"
      body="No stock records are loaded. HAVØK ships bikes themselves from DTLA. This room stays empty until real inventory is wired."
    />
  );
}
