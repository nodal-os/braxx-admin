import { EmptyRoom } from "@/components/workspace";

export default function InventoryUpdatePage() {
  return (
    <EmptyRoom
      kicker="Inventory"
      title="Update stock"
      body="No adjustment form is connected. Stock changes stay off until inventory is wired."
    />
  );
}
