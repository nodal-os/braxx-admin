import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function SocialPage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Social"
      body="No social accounts or posts are connected. This room stays empty until a live feed is wired."
    />
  );
}
