import { EmptyRoom } from "@/components/workspace/EmptyRoom";

export default function PerformancePage() {
  return (
    <EmptyRoom
      kicker="Command"
      title="Performance"
      body="No live KPIs are connected. Shopify is checkout-only. This room stays empty until real commerce data is wired."
    />
  );
}
