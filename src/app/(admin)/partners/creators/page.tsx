import { EmptyRoom } from "@/components/workspace";

export default function PartnerCreatorsPage() {
  return (
    <EmptyRoom
      kicker="Partners"
      title="Creators"
      body="No creator partnerships are loaded. This roster stays empty until real creators are added."
    />
  );
}
