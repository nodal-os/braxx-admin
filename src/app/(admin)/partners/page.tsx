import { EmptyRoom } from "@/components/workspace";

export default function PartnersPage() {
  return (
    <EmptyRoom
      kicker="Partners"
      title="Network"
      body="No partner rows are loaded. HAVØK ships bikes from DTLA. Dealer and creator directories stay empty until real partners are added."
    />
  );
}
