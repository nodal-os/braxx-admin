import { EmptyRoom } from "@/components/workspace";

export default function PartnerDealersPage() {
  return (
    <EmptyRoom
      kicker="Partners"
      title="Dealers"
      body="No dealers are loaded. This directory stays empty until real dealer records exist."
    />
  );
}
