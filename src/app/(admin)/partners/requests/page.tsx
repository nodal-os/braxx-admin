import { EmptyRoom } from "@/components/workspace";

export default function PartnerRequestsPage() {
  return (
    <EmptyRoom
      kicker="Partners"
      title="Requests"
      body="No incoming applications are loaded. Approve and reject stay off until real requests arrive."
    />
  );
}
